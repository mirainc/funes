version: '2'
services:
  nginx:
    build:
      context: ./
      dockerfile: Dockerfile.dev
    ports:
    - "80:80"
    - "443:443"
    - "3128:3128"
    volumes:
    - ./:/usr/src/app/:rw
    command: bash -c "make run"
    # command: bash -c "sleep infinity"
    environment:
    - HELLO=WORLD
